#include <stdio.h>
#include <stdlib.h>
/**
 * main - the start of the program
 * @argc: argument count
 * @argv: argument vector
 * Return: Always Success (0)
 */
int main(int argc, int  **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
