#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: the first number
 * @b: the second number
 * Return: the sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
