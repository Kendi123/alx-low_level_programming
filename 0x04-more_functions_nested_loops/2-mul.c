#include "main.h"
/**
 * mul - multiplies two integers
 * @a: integer 1
 * @b: integer 2
 * Return: the product of the two
 */
int mul(int a, int b)
{
	int product;

	product = (a * b);
	return (product);
}
